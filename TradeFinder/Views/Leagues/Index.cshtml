@model IEnumerable<TradeFinder.ViewModels.LeagueView>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table table-condensed table-hover table-responsive table-striped">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.League.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.LeagueHost.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.League.AddedOn)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.League.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.LeagueHost.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.League.AddedOn)
        </td>
        <td>
            @Html.ActionLink("Trades", "Index", "Trades", new { id = item.League.LeagueId }, null) |
            @Html.ActionLink("Teams", "Index", "Teams", new { id = item.League.LeagueId }, null) |
            @Html.ActionLink("Edit", "Edit", new { id=item.League.LeagueId }) |
            @Html.ActionLink("Details", "Details", new { id=item.League.LeagueId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.League.LeagueId })
        </td>
    </tr>
}

</table>
